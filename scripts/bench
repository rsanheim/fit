#!/usr/bin/env bash
# Legacy script - delegates to bench-scaling for backward compatibility
# For new usage, prefer:
#   bench-all       - head-to-head comparison of all implementations
#   bench-scaling   - worker scaling for single implementation
#   bench-suite     - full benchmark suite with markdown output
exec "$(dirname "$0")/bench-scaling" rust status "${1:-1,2,4,8,12,16}"
